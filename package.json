{
  "name": "erc8004-verify",
  "private": true,
  "scripts": {
    "dev": "bunx shadow-cljs watch app",
    "build": "bunx shadow-cljs release app && bun run dist",
    "dist": "rm -rf dist && mkdir -p dist/js && cp public/index.html public/ERC8004-SKILL.md public/favicon.svg dist/ && cp public/js/main.js dist/js/",
    "serve": "bunx serve dist"
  },
  "dependencies": {
    "viem": "^2.45.1",
    "zustand": "^5.0.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "shadow-cljs": "^2.28.0"
  }
}
